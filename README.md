# FrameOak UI

This is a proof of concept of themable UI framework built on top of [elm-ui](https://package.elm-lang.org/packages/mdgriffith/elm-ui/latest/)

## ToDo / TBD
1. Define proper `Theme` and `ThemeConfig` structure
2. Expose `Internal.Oak.Theme`, `Internal.Oak.ThemeConfig`, `Internal.Oak.generateTheme` and `Oak.Theme.Custom` to allow creating custom themes / overriding parts of default themes
3. Create list of components to build
4. Create nice design (with help of designer, I guess)
